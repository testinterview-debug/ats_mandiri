package com.example.test_moviedb.core.common

/**
 * Base interface for UI State
 */
interface UiState

/**
 * Base interface for UI Events (user actions)
 */
interface UiEvent

/**
 * Base interface for UI Effects (one-time actions like navigation or toasts)
 */
interface UiEffect